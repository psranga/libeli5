plugins {
        id "com.linkedin.python-cli" version "0.3.36"
        id "com.linkedin.python-sdist" version "0.3.36"
}

python.pex.fatPex = true

dependencies {
             python 'pypi:appdirs:1.4.0'
             python 'pypi:packaging:16.8'
             test 'pypi:mock:1.3.0'
}

repositories {
             pyGradlePyPi()
}